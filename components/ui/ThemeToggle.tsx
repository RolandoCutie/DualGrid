'use client';

// Dark mode is permanent — this component is kept for backward compatibility
// but renders nothing since there is no theme toggle functionality.
export default function ThemeToggle() {
  return null;
}
